import { render } from "@testing-library/react";
import SwitchThemeMode from "./SwitchThemeMode";

describe("Component SwitchThemeMode", () => {
  let wrapper;
  beforeEach(() => {
    wrapper = render(<SwitchThemeMode />);
  });
  it("Should toggle to dark mode", () => {
    
  });
});
